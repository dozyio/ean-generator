import React from 'react';
import App from './App';

const AppWrapper = function AppWrapper() {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default AppWrapper;
